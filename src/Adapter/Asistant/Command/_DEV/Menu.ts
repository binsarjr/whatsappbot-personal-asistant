// import Command from '../../../Infrastructure/Command'

// Asistant().command.register({
//     pattern: /^\.\s{0,}menudev$/is,
//     events: ['chat-update'],
//     handler: async function ({ chat }) {}
// })
